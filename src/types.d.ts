//the name "types.d.ts" use the leter "d" because only can aford definitions and no functions

export interface Sub {
  nick: string;
  avatar: string;
  subMonths: number;
  description?: string;
}
//setting types in array
